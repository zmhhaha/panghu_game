apiVersion: v1
kind: ConfigMap
metadata:
  name: shapan-config
  namespace: shapan
data:
  NODE_ENV: production
  TRUST_PROXY_AUTH_HEADERS: "true"
  PROVIDER: fallback
  SIM_TICK_MS: "5000"
  AGENT_POLL_MS: "2000"
  SSE_POLL_MS: "2000"
