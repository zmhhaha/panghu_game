# ============================================================
#  Agent 通用配置（所有 Python FastAPI Agent 共用）
#  通过 envFrom.configMapRef 注入
# ============================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-config
  namespace: school-of-one
data:
  PROVIDER: "deepseek"
