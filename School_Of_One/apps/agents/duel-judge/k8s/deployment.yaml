# ============================================================
#  Duel Judge — API 服务（K8s 部署 - school-of-one 命名空间）
#  通过 deploy/k8s/ai-service.yaml 注入 agent-config（PROVIDER）
#  通过 llm-secret 注入 LLM 凭据
#  部署:
#    kubectl apply -n school-of-one -f k8s/deployment.yaml
# ============================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: duel-judge
  namespace: school-of-one
  labels:
    app: duel-judge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: duel-judge
  template:
    metadata:
      labels:
        app: duel-judge
    spec:
      containers:
      - name: api
        image: arm-cluster-master:5000/duel-judge:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
        envFrom:
        - configMapRef:
            name: agent-config
            optional: true
        env:
        - name: DEEPSEEK_API_KEY
          valueFrom:
            secretKeyRef:
              name: llm-secret
              key: deepseek-api-key
              optional: true
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: llm-secret
              key: openai-api-key
              optional: true
        - name: CUSTOM_API_KEY
          valueFrom:
            secretKeyRef:
              name: llm-secret
              key: custom-api-key
              optional: true
        - name: CUSTOM_API_BASE
          valueFrom:
            configMapKeyRef:
              name: agent-config
              key: CUSTOM_API_BASE
              optional: true
        - name: CUSTOM_MODEL
          valueFrom:
            configMapKeyRef:
              name: agent-config
              key: CUSTOM_MODEL
              optional: true
        resources:
          requests: { cpu: "200m", memory: "256Mi" }
          limits: { cpu: "2", memory: "2Gi" }
        livenessProbe:
          httpGet:
            path: /api/duel/health
            port: 8003
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/duel/health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: duel-judge
  namespace: school-of-one
spec:
  selector:
    app: duel-judge
  ports:
  - port: 8003
    targetPort: 8003
