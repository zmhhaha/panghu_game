# ============================================================
#  Duel Judge 通用配置
#  用法: sed "s/__NAMESPACE__/duel-judge/g" k8s/configmap.yaml | kubectl apply -f -
# ============================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: duel-judge-config
  namespace: __NAMESPACE__
data:
  PROVIDER: "deepseek"
  # PROVIDER: "openai"
  # PROVIDER: "custom"
  # CUSTOM_API_BASE: "http://0.0.0.0"
  # CUSTOM_MODEL: "deepseek-chat"
