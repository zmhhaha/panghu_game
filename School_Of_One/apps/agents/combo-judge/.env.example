# 模型调用统一走集群内 llm-service；本服务不再持有任何 provider 凭据。
# 部署到 K8s 时由 k8s/deployment.yaml 与 deploy/k8s/ai-service.yaml 注入，不需要这个文件。
LLM_BASE_URL=http://llm-service.llm.svc.cluster.local/v1
LLM_MODEL=deepseek-guarded
LLM_SERVICE_TOKEN=

# LLM_MODEL 是 llm-service 注册的**别名**，不是上游模型名。
# guarded 档禁 tools / response_format，但会做不可信内容分隔（spotlight）与 canary 泄漏检测 ——
# 三个 judge 的输入都是玩家写的自由文本，就该用这一档。
