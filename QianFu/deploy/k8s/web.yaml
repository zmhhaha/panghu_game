apiVersion: apps/v1
kind: Deployment
metadata:
  name: qianfu-web
  namespace: qianfu
  labels: { app: qianfu-web }
spec:
  replicas: 2
  selector:
    matchLabels: { app: qianfu-web }
  template:
    metadata:
      labels: { app: qianfu-web }
    spec:
      containers:
        - name: web
          image: arm-cluster-master:5000/qianfu-web:latest
          imagePullPolicy: Always
          ports: [{ name: http, containerPort: 3000 }]
          readinessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: qianfu-web
  namespace: qianfu
spec:
  selector: { app: qianfu-web }
  ports: [{ name: http, port: 3000, targetPort: http }]
---
apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: qianfu
spec:
  selector: { app: qianfu-web }
  ports: [{ name: http, port: 80, targetPort: http }]
