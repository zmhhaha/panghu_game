apiVersion: batch/v1
kind: Job
metadata:
  name: qianfu-db-migration
  namespace: qianfu
spec:
  backoffLimit: 3
  template:
    metadata:
      labels: { app: qianfu-db-migration }
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: arm-cluster-master:5000/qianfu-server:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-ec"]
          args:
            - for file in /app/apps/server/migrations/*.sql; do psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$file"; done
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: qianfu-database
                  key: url
