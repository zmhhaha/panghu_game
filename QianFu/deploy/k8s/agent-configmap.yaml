apiVersion: v1
kind: ConfigMap
metadata:
  name: qianfu-agent-config
  namespace: qianfu
data:
  # Values: fallback, openai, deepseek, anthropic, custom.
  # Credentials and connection fields are injected from the Vault Secret.
  PROVIDER: fallback
